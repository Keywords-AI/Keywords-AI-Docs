---
title: "OpenRouter"
description: "Use your own OpenRouter credits through Keywords AI"
icon: "lock-keyhole-open"
---

There are 2 ways to add your OpenRouter credentials to your requests:
## Add it from the UI 

<Steps>
<Step title="Go to the Credentials page">
Go to [Credentials page](https://platform.keywordsai.co/platform/api/credentials)
<img height="200" width="500"  src="/images/bring-own-key.png" alt="Dashboard Page"/>
</Step>
<Step title="Add your OpenRouter credentials.">

<img height="200" width="300"  src="/images/providers/openrouter.png" alt="Dashboard Page"/>
</Step>
</Steps>

## Add it in code
- Add `customer_credentials` parameter in your [request body](/api-endpoints/integration/chat-completions) to use your own OpenRouter credits.

```json
{
  // Rest of the request body
  "customer_credentials": {
    "openrouter": {
      "api_key": "YOUR_OPENROUTER_API_KEY",
    }
  }
}
```

## Full request example
<Accordion title="Example">
<CodeGroup>
```python openai_example.py
from openai import OpenAI

client = OpenAI(
    base_url="https://api.keywordsai.co/api/",
    api_key="YOUR_KEYWORDSAI_API_KEY",
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role":"user", "content":"Tell me a long story"}],
    extra_body={"customer_credentials": {
                  "openrouter": {
                      "api_key": "YOUR_OPENROUTER_API_KEY",
                  }
                }
              }
)
```
```python api_example.py
import requests
def demo_call(input, 
              model="gpt-4o",
              token="YOUR_KEYWORDS_AI_API_KEY"
              ):
    headers = {
        'Content-Type': 'application/json',
        'Authorization': f'Bearer {token}',
    }

    data = {
        'model': model,
        'messages': [{'role': 'user', 'content': input}],
        'customer_credentials': {
            'openrouter': {
                'api_key': "YOUR_OPENROUTER_API_KEY",
            }
        }
    }

    response = requests.post('https://api.keywordsai.co/api/chat/completions', headers=headers, json=data)
    return response

messages = "Say 'Hello World'"
print(demo_call(messages).json())
```
</CodeGroup>
</Accordion>
