## User update
Update existing users to have better user observability.

### Update users on the platform
You can update user's name and email on the platform. Go to the [Users page](https://platform.keywordsai.co/platform/users) and click on the user you want to update. Type user's name and email in the corresponding fields.

<img width="300" src="/images/users-page/user-edit.png" alt="Users Page data" />

### Update users through API

<Note>Use the [User Update endpoint](/api-endpoints/async-endpoints/user-update-endpoint). instead of our other endpoints</Note>
```
url = "https://api.keywordsai.co/api/user/update/some_customer_identifier"
```
 <CodeGroup>
```Python Python
import requests

url = "https://api.keywordsai.co/api/user/update/some_customer_identifier" #replace with the actual customer_identifier to update
api_key = "YOUR_KEY" # Replace with your actual Keywords AI API key
data = {
    "name": "John Doe",
    "period_budget": 30
    # Add other parameters here
}
headers = {
    "Authorization": f"Bearer {api_key}",
    "Content-Type": "application/json"
}

response = requests.patch(url, headers=headers, json=data)
print(response.json())
```

```TypeScript TypeScript
// Define the function with TypeScript
fetch("https://api.keywordsai.co/api/user/update/some_customer_identifier", { 
    // replace with the actual customer_identifier to update
method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer Your_Keywords_AI_API_Key '
  },
    body: JSON.stringify({
        name: "John Doe",
        period_budget: 30,
        // Add other parameters here
    })
})
.then(response => response.json())
.then(data => console.log(data));
```
 </CodeGroup>
